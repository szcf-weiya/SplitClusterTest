# estimate the covariance matrix (assuming there are two clusters)

estimate the covariance matrix (assuming there are two clusters)

## Usage

``` r
est.Sigma(x)
```

## Arguments

- x:

  data matrix

## Value

estimate covariance matrix
